# Gap Matrices — ManyChat Pro + HighLevel vs Current ConvoxOS (2026-08-31)

> Priority: P0 correctness/foundational · P1 core · P2 major parity · P3 valuable · P4 later. Evidence VERIFIED|INFERRED|UNCERTAIN per source discipline.

## 1. Feature Gap Matrix

| Capability | ManyChat Pro | HighLevel | Current ConvoxOS | Target ConvoxOS | Priority | Evidence |
|------------|--------------|-----------|------------------|---------------|----------|----------|
| **Builders** | Flow Builder (visual) + Basic Builder (linear) toggle over one canonical model; preview + Set Live | Workflow Builder Infinite Canvas + Draft/Publish toggle + Stats View + Version History | Flows `@xyflow/react` canvas + Automations `builder-tree` sequential; Basic vs Flows distinct persistence; no toggle; no minimap on automations; no Stats View/History | One product surface with two editors over canonical per-engine graph; add minimap, toggle, dirty/publish split, Stats View, History | P2 | manychat `14281166306332`, GHL `155000001254`; repo `src/components/flows/flow-canvas.tsx:1` vs `src/components/automations/automation-builder.tsx:21` |
| **Triggers** | Starting Step channel-specific; keyword (6 rules) + Default Reply + Basic triggers; Sequences/Rules global triggers | 80+ across 14 categories (Contact/Events/Appointments/Opportunities/...); multiple OR triggers | 8: new_message, first_inbound, keyword_match, new_contact, conversation_assigned, tag_added, time_based, interactive_reply | P1: +contact_changed/dnd, note/task added, customer_replied, inbound_webhook, appointment_*, opportunity_*; P2: 80+ full parity | P1 | `src/types/index.ts:465` vs GHL `155000002292`; ManyChat `14281211785884` keywords |
| **Messages (Text)** | Message content node with text+optional buttons; available per channel | Send Email/SMS/Messenger/IG/WhatsApp as communication actions | send_message per channel_target current|WA|TG both engines | Keep; add channel-aware typing toggle | P0 (already) | `src/lib/flows/types.ts:30` |
| **Media** | Image/Video/Audio/File/PDF/Gallery/Card per channel matrix | Communication actions carry media where applicable | send_media (flow only image/video/document) ; TG media via sendTelegramMedia | Promote send_media to both engines; add File/Audio per matrix | P1 | `src/lib/flows/types.ts:88` |
| **Buttons** | Text up to 3 WA/IG/Messenger vs 10 TG; types depend on channel+block; connection per button | Chained via interactive_reply trigger across workflows | send_buttons both hosts; validate caps WA-only (≤3) incorrectly applies to TG | Channel-aware cap (WA≤3 TG≤10) per channel_target | P0 | `14281157003292` vs `src/lib/flows/validate.ts:362` |
| **Lists** | 10 rows total across sections; row limit 24/72 | Similar via list actions | send_list both hosts (WA native, TG flattened keyboard) | Fix cap channel-aware | P0 | same |
| **Data collection** | Data Collection block type per channel, 30-min wait, multiple choice + not-responded branch, image uploads not previewable | Forms/Surveys as inbound triggers (not inline) | collect_input (flow only) free-text → var, validation ignored | Add timeout branch + validation enforcement (email/phone/regex) + channel variants | P2 | `14281167138588` vs `src/lib/flows/types.ts:124` |
| **Conditions** | General filters: Tag, Widget/Ad/API opt-in, List, Sequence, Current time, Segments, User/Bot Fields with date offset + all/any | If/Else evaluated on contact fields | Flow condition var|tag|contact_field single predicate; Automations tag_presence|contact_field|message_content|time_of_day | Unify + multi-condition AND/OR + segment/sequence filters | P1 | `14281142518556` vs `src/lib/flows/types.ts:158` |
| **Waits** | Smart Delay Duration (with continue-between + day filter) / Date Specific/Dynamic + offset + contact TZ → account fallback + admin pause adds + no reopen 24h | Wait Step + Goal + business hours via condition | wait fixed amount minutes|hours|days only, shared pending table | Expand to Duration+Date+window+day+TZ + Goals | P1 | `14281197046812` vs `src/lib/flows/engine.ts:159` |
| **Goals** | — | Goal Event skip-ahead | Not present | New Goal node (wait bypass) | P2 | GHL `155000002294` Internal Tools |
| **Randomizer** | Randomizer 2–6 variations, sticky vs every-time, batch distribution quirk | Split test A/B | Not present | Randomizer 2–6 sticky/every | P2 | `14281151100060` |
| **Sequences** | Subscribe/Unsubscribe sequence; per-message enabled; delays + between window | Drip Mode batch dripping | No domain object; only add_tag branch could emulate | First-class `sequences` domain P2 | P2 | `14281202572316` |
| **Broadcasts** | Campaign: audience conditions + channel + Send Now/schedule (recipient frozen at send) + clone from automation | Workflow comms with broadcast resilience via delivery_locked_at | Template-only broadcasts with `delivery_locked_at` `038` + resilience | Add audience segmentation + channel selection P2 | P2 | `14281228205212` |
| **CRM actions** | Tags, User/Bot Fields, Sequences, Delete Contact, Opt-in/out, Bot Field, Conversion, Assign/Notify etc. | 15 Contact actions + Opportunities/Tasks etc. | add/remove_tag, update_contact_field (built-in+custom), assign_conversation, create_deal, close_conversation | +add_note, add_task (needs table), update_opportunity/move_stage, DND | P1 | `17636378650268` + GHL list |
| **Opportunities** | Not ManyChat concept | Create/Update/Remove Opportunity, Pipeline Stage Changed | pipelines/stages/deals CRUD only; create_deal only | +Update/Move/Remove + stale trigger | P1 | `src/types/index.ts:358` |
| **Tasks/Notes** | Not explicit | Add Task (contact-less allowed), Task triggers, Add Note | contact_notes table exists but no node; tasks missing | New tasks table + nodes + triggers | P1 | NOT FOUND tasks |
| **Assignments** | Assign conversation + Notify | Assign/Remove Assigned User, Edit Conversation | assign_conversation + handoff | Add remove-assigned + edit conversation | P3 | `017` sharing |
| **Conversations** | N/A | Edit Conversation, Conversation AI, Manual Action | Open/pending/closed + handoff + close | Conversation AI action P3 | — | `src/types/index.ts:164` |
| **Channels** | Content blocks per channel matrix; channel-specific triggers never fire on mismatch | Comm actions per channel; integration locks until connected | WA native + TG plug; messages.channel provenance; ChannelSocket thin | Keep WA+TG scope; IG/Messenger future via same pattern (add CHECK) | P0 for WA+TG; P4 IG/MM | `src/lib/channels/socket.ts:1` |
| **Templates** | Message Templates (not in this matrix beyond channel) + reusable automations | Recipes (templates) | WA approved templates via meta-api; 3 flow templates static | Keep; expand gallery P2 | P2 | `src/lib/flows/templates.ts:1` |
| **Reusable automation** | Start Automation sync (no input wait) + copy/paste between accounts | Add to Workflow + Go To + Remove + Pass Input Trigger Parameters | No explicit reuse node; chained via interactive_reply + tag | Start Automation / Add to Workflow P2 | P2 | `14281157602716`, GHL `155000002554` |
| **Testing/Preview** | In Manychat (widget, limited) + In messengers (native, full, SMS/email charges) | Test Workflow with contact → Execution Logs; Stats drill | Automations dry-run + test-dialog; Flows no dry-run; no widget | Add quick widget + Flows dry-run + contact-selector full test | P2 | `14281198254620`, GHL `155000001254` Testing |
| **Publishing / Versioning** | Set Live; 30-block auto-pause; recipient list frozen at send; no documented Version History | Save vs Publish independent; Version History snapshots; waiting remain at step | is_active/status active; no draft/publish split; no history; 30-block not enforced | Separate Saved≠Published + Version History + auto-pause cap | P2 | manychat FAQ 30, GHL `155000001254` |
| **Analytics/Execution history** | Per-step stats + drop-off filterable by trigger; Smart Delay Overall/Waiting/Passed + Unique | Trigger Attempted/Matched/Unmatched + per-action delivery/engagement 30-day; delete retains stats | flow_run_events + automation_logs.steps_executed; no stats view | Stats View 30-day + per-trigger drill + Unique | P2 | `155000001254` Stats View |
| **AI** | AI Flow Builder assistant 8-step chat (WHATSAPP/IG/Messenger/Telegram) costed | AI Prompt (GPT-3) action + AI Assistant suggestions | AI auto-reply/knowledge `029/030`; no workflow AI action | Workflow AI Prompt action P3 | P3 | `14281200017948`, GHL AI Prompt |
| **Webhooks/HTTP** | Dynamic block + External Request (GET/POST/PUT/DELETE) + fallback branch; logs in Settings→Logs | Webhook/Custom Webhook with trigger-dependent payload; Google Sheets | send_webhook POST only 10s + SSRF guard; http_fetch DB CHECK but no runtime | Expand to full External Request + method + fallback | P2 | `14281268533788`, GHL `155000003299` |
| **Integrations** | Integrations category adds actions; Zapier/Google Sheets etc. | Marketplace actions (Slack, Sheets, Todoist, etc.) | Webhook only; webhook endpoints `028` + deliver `deliver.ts` | Sheets/Todoist as needed P3 | P3 | `src/lib/webhooks/*` |

## 2. Node Gap Matrix

| Node | ManyChat behavior | HighLevel behavior | Current ConvoxOS | Required target | Gap | Pri | Evidence |
|------|-------------------|--------------------|------------------|-----------------|-----|-----|----------|
| **Trigger** | Starting Step, channel MUST match channel of message nodes or never fires (WA vs IG mismatch) `⚠️ Note` | Multiple OR triggers; filters optional; trigger payload context-dependent | 8 types, single trigger per automation/flow; channel filter only on keyword/interactive | P1: 15+ types; P2: multi-OR triggers | Catalog thin; multi-trigger missing | P1 | `src/lib/flows/engine.ts:190` |
| **Send Message** | Text (+ buttons) inside message node; typing delay optional; channel-specific block | Comm action Send X per channel | Text with channel_target current|WA|TG both hosts | Hide TG typing toggle; add delay blockInside | — | — | `src/lib/flows/types.ts:30` |
| **Send Media** | Image/video/audio/file/PDF/card/gallery per channel matrix `14281196200604` | Media inside comm actions | image|video|document only (flow only) | Both hosts + Audio/File per matrix | Media kinds incomplete | P1 | `src/lib/flows/types.ts:88` |
| **Buttons** | 1–3 WA/Messenger/IG vs 10 TG; Call Number Messenger-only; types filtered | Interactive via workflows not explicit buttons | Same WA limit applied to TG | Channel-aware cap | TG valid builds blocked | P0 | `src/lib/automations/validate.ts:76` |
| **Lists** | 10 rows total; row title/desc limits | Similar | Same with TG flatten transform | Channel-aware | Same bug | P0 | same |
| **Data Collection** | Custom User/Bot Field store; multiple reply types; 30-min pause; not previewable | Form/Survey submitted triggers (not inline) | free-text → var only; no timeout branch | Add timeout + multi/validation | Inline missing | P2 | `src/lib/flows/types.ts:124` |
| **Condition** | General+Fields multi with all/any `Does the contact match` `14281142518556` | If/Else with tag/field/message/time subjects | Single predicate | AND/OR multi | Single vs multi | P1 | `src/lib/flows/types.ts:158` |
| **Wait** | Smart Delay Duration Date window+days+TZ + 24h window caveat; stats Overall/Waiting/Passed | Wait same + Goal + business hours | Fixed duration only | Full Smart Delay + Goal | Variants missing | P1 | `14281197046812` |
| **Randomizer** | 2–6 variations % + sticky vs every-time `14281151100060` | Split | Not present | 2–6 sticky/every | Missing | P2 | — |
| **Start Automation** | Sync call — runs callee all messages then returns without waiting for input | Add to Workflow + Pass Input params + Go To | No callee node; only trigger chaining via interactive_reply | Start Automation node + Add to Workflow | Missing | P2 | `14281157602716` |
| **Tags** | Add/Remove Tag inside Action block multi-action | Add/Remove Tag + Copy Contact | add_tag/remove_tag via set_tag flows | Keep + multi-action per block | Multi-action not yet | P3 | `src/lib/automations/unified-taxonomy.ts:102` |
| **Contact Fields** | Set/Clear User/Bot Field + Delete Contact + Opt-in/out | Update Contact Field + Find + DND + Copy + Delete | update_contact_field custom:`<id>` + built-ins name/email/company | Add clear + delete + DND | Delete/opt missing | P3 | `src/lib/automations/engine.ts:578` |
| **CRM Actions (Deal)** | — | Create/Update/Remove Opportunity + stage moves | create_deal only | +update/move/remove | update/move missing | P1 | `src/types/index.ts:577` |
| **Opportunity** | — | Same | create only | full | same | P1 | — |
| **Pipeline** | — | Same | stages CRUD but no move node | move | same | P1 | `src/components/pipelines/*` |
| **Task** | — | Add Task (contact-less allowed) + Task triggers `155000002292` Contact | None | new tasks table + node | Missing entity | P1 | NOT FOUND |
| **Note** | — | Add Note | table exists `001` but no node | Add Note node | Node missing | P1 | `supabase/migrations/001_initial_schema.sql:1` |
| **Assignment** | Assign + Notify Assignees (email/SMS) | Assign/Remove Assigned; Edit Conversation | assign_conversation placeholder RR | +remove +notify | Incomplete | P3 | `src/lib/automations/engine.ts:545` |
| **Webhook** | Dynamic block remote message + External Request w/ fallback + Settings→Logs | Webhook Custom + Sheets + trigger-dependent payload | POST only | +method + fallback + sheets | Limited | P2 | `src/lib/automations/engine.ts:649` |
| **HTTP** | Same as Webhook via External Request | Same | same via send_webhook | full | same | P2 | — |
| **AI** | AI assistant 8-step chat | AI Prompt (GPT-3) | AI reply on inbound path only | AI Prompt workflow action | Missing | P3 | `src/lib/ai/auto-reply.ts:1` |
| **Goal** | — | Goal Event skip-ahead | Not present | New Goal node | Missing | P2 | — |
| **Sequence** | Sequence entity per-message enabled + delays + windows `14281202572316` | Drip Mode | None | New `sequences` domain | Missing | P2 | — |
| **Broadcast** | Clone from automation; audience tag/conditions; channel Any | No broadcast — comm actions per workflow | Template-only with recipients + resilience | Add audience + channel selection | Audience missing | P2 | `supabase/migrations/001_initial_schema.sql` broadcasts |

## 3. Booking Summary — Priority Rollup

- **P0 correctness (publish would produce wrong runtime):** TG buttons/list caps channel-aware validation (2 caps matrix). Also channel_target missing loud error already correct — no further.
- **P1 core product gaps:** Trigger catalog expansion (contact/task/note/appointment/opportunity), CRM task entity+nodes, note node, deal update/move, condition multi-predicate, full Wait variants window/date.
- **P2 major parity:** Randomizer/Goal, Sequences, Broadcasts audience, External Request full, Stats View + Version History, Testing quick widget, Reusable automation Start/Add, Activity feed.
- **P3 valuable:** Delete contact/DND/Opt, Assignee notify, Google Sheets, Workflow AI prompt, Appointment if calendar owned.
- **P4 later:** Channels Instagram/Messenger/SMS/Email, Company workflows, Ecommerce/IVR/Courses.
